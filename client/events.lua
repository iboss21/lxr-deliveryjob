-- Dynamic world events (client side): weather, stealth checks
CreateThread(function()
    while true do
        -- placeholder for future: apply handling multipliers based on weather
        -- You can read weather hash and adjust vehicle handling here.
        Wait(2000)
    end
end)
